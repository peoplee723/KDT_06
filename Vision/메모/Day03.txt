Day03


이미지 간단한 전처리 코드?

패딩 -> valid(기본), same
conv
1d -> 필터가 좌우로만 이동(
2d -> 필터가 좌우상하이동 (2d이미지) -> (배치크기,채널,H(행),W(열))
3d->필터가 3방향 이동 (3d이미지들)

CNN -> 피쳐를 고르는 고민 X -> 어떻게 해야 특징들을 잘 뽑게 만들까?


모델 구성 과정
특징 추출 -> 학습
-특징 추출
conv2d (in, out ,kernel_size ,stride) 
		(a,b,c,d) -> 
pool2d(커널 사이즈, stride)

=> 함성곱과 풀링의 조합

- 학습


==> 일반적으로 커널 사이즈는 커졌다가 작아지는 형태
Mnvision -> 현대자동차 납품

정사각형일때,
stride=1,
-> (크기-커널 크기)+1
stride>1,
-> 크기 /stride +1

