Day02

numpy

벡터연산 수행(축 연산)

다차원에 변수 하나 할당하면 공유

행렬 랭크 -> 내부 벡터들이 서로 조합해서 구성할 수 없는 벡터의 개수
[[1,2,4],[2,4,8],[3,4,5]] ->1,2행은 1행*2 -> 독립아님

정사각행렬-> trace()= 대각선합

np.dot()-> 행렬곱


ch.3
메소드로 검색
.item(인덱싱), .view 로 검색 가능
.itemset(인덱싱 또는 좌표, 바꿀값) 으로 원소 교체 가능

.take(인덱싱)->검색
.put(인덱싱, 바꿀값)-> 교체

.choose(행인덱스번호, 데이터)-> 데이터의 행조회

.where(조건) 조건에 해당하는 행과 열의 인덱스 정보를 반환
		->인덱싱에 조건 삽입시 원소 반환

.select(조건, 데이터) ->조건 결과 반환

ch.4
브로드캐스팅
차원이 다른 벡터간 연산을 위해 shape를 동일하게 만드는 것
