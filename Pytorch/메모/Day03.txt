Day03


프레임워크
- 개발에 필요한 모든것을 포함하고 있는 집합체
Ex)	ML알고리즘, 성능평가, 전처리, 데이터셋, 특성공학...패키지

인터페이스
- 서로 다른 요소/장치/사람을 연결시켜주는 것
Ex)	UI (사람과 연결) -> 버튼, 그림, 글자 입력 등으로 연결
	OOP개념에서 서로 다른 객체들이 동일 기능을 사용할 수 있도록 해주는 기능

모듈
- 같은 목적의 변수, 함수, 클래스를 포함한 1개의 파일
Ex)	웹에 요청 목적의 변,함,클
	웹에 받은 응답 처리 목적의 변,함,클
	웹 주소 분석 목적의 변,함,클

패키지
- 같거나 유사한 분야의 모듈을 모아서 하나로 만든 것
Ex)     요청 처리 모듈, 응답 처리 모듈, 주소 분석 모듈....=> 웹 패키지


IDE (통합개발환경)
- 개발에 필요한 모든 도구들을 제공해주는 SW


nlp

모델 설계
입력층: 입력=피쳐, 퍼셉트론 개수=10(개발자 설정), 통로 설정AF(activation function)= ReLU() (개발자 설정)
   l   (ReLU라는 통로(0이하는 0, 나머지는 그대로)를 통해 퍼섭트론 결과값이 전달됨)
   V  (param 수= 퍼셉트론의 수 * (입력받은 수 + 절편))
은닉층: 입력=10, 퍼셉트론 수=5, 통로 설정AF= ReLU()

출력층: 입력=5, 타겟 수=1 , 통로= 값의 성격에 따라 sigmoid or softmax
	마지막 

+층 or +뉴런 ==> 층을 늘리는게 성능이 더 좋음(일반적)

torch.FloatTensor(X_train[start:end].values).to(DEVICE)
   텐서화		배치 사이즈만큼 추출    cpu에 
===> 추출한 데이터에서 피쳐와 타겟 변환